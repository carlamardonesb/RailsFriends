<!-- we already have a notice in the aplication file <p id="notice"><%= notice %></p> -->
<% if user_signed_in? %>
<h1>Friends List</h1>

  <table class="table table-striped table-bordered table-hover">
    <thead class="table-dark">
      <tr>
        <th>Name</th>
        <!-- <th>Last name</th> -->
        <th>Email</th>
        <th>Phone</th>
        <th>Twitter</th>
        <!-- <th>User Id</th> -->
        <th></th>
        <!-- <th colspan="2"></th> :this puts the amount of columns to divid into-->
      </tr>
    </thead>

    <tbody>
      <% @friends.each do |friend| %>
        <!-- so only the friends that are associated to that user show -->
        <% if friend.user == current_user %>
          <tr>
            <td>
              <!-- <%= link_to 'Show', friend %> -->
              <!-- <%= friend.first_name %> -->
              <!-- <%= friend.last_name %> -->
              <%= link_to friend.first_name + ' ' + friend.last_name, friend %>
            </td>
            <td><%= friend.email %></td>
            <td><%= friend.phone %></td>
            <td><%= friend.twitter %></td>
            <!-- <td><%= friend.user_id %></td> -->
            <!-- <td><%= link_to 'Show', friend %></td> -->
            <!-- % if friend.user == current_user % -->
            <!-- <td><%= link_to 'Edit', edit_friend_path(friend) %></td> -->
            <td><%= link_to 'Delete', friend, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-outline-danger btn-sm"%></td>
            <!-- % else % -->
            <!-- <td></td>-->
            <!-- <td></td>-->
            <!-- % end % -->
          </tr>
        <% end %>
      <% end %>
    </tbody>
  </table>

<br>

<!-- add a button with bootstrap: you just need the class associated with the button 
<button type="button" class="btn btn-secondary">Secondary</button>
-->

<%= link_to 'New Friend', new_friend_path, class:"btn btn-secondary" %>

<% else %>

<h1>Home world!</h1>
This is my first Rails Webpage...

<% end %>